requests>=2.31
python-dotenv>=1.0
openai>=1.0
anthropic>=0.34
groq>=0.4
beautifulsoup4>=4.12
lxml>=5.0  # 2026-08: ats_scrapers.py's 4 BeautifulSoup() call sites switched
           # from "html.parser" (pure-Python, slowest option) to "lxml"
           # (C-accelerated) — same find()/select()/get_text() API, just
           # much faster parsing per page.

# node.py (the shared crawl/detect/write engine every seed source — kaggle_probe.py,
# github_org_probe.py, tranco_crawl.py, common_crawl_probe.py — imports directly) and
# crawl_ii.py (Crawl II's heuristic archive_ii scraper) both need aiohttp/selectolax
# for the async fetch/parse step. Not needed by crawl_i.py/discovery.py themselves.
aiohttp>=3.9
selectolax>=0.3
# aiodns — concurrent async DNS resolution, used by node.py's crawl engine
# at high concurrency (see new_connector()). Optional — the script falls
# back to aiohttp's default resolver if missing, just slower under heavy load.
# 2026-09: also now powers the archive_ii quality gate's DNS MX-record
# provider check (see _mx_provider_score() in node.py) — same graceful
# degradation if missing, that one signal is just skipped (0 score impact).
aiodns>=3.0

# duckdb removed 2026-08 — was only needed by the retired host-crawl
# pipeline's Common Crawl Host Index Parquet queries (see ./host crawl/).
# Tranco seeding uses only stdlib zipfile/csv/io + requests, no DuckDB.

# psycopg2-binary — needed by ctlog_extract.py's direct guest-Postgres
# connection to crt.sh (certwatch). Binary wheel, not the source
# psycopg2 package, so GitHub Actions runners don't need libpq-dev
# installed separately.
psycopg2-binary>=2.9

# pyarrow — needed by discovery.py's Latmay H.F / Edward H.F sources
# (2026-09) to read Hugging Face's auto-converted Parquet dataset
# exports, with column projection so Edward's heavy unused columns
# (description_html etc.) are never pulled into memory.
pyarrow>=15.0
