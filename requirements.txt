requests>=2.31
python-dotenv>=1.0
openai>=1.0
anthropic>=0.34
groq>=0.4
beautifulsoup4>=4.12

# host_crawl.py / host_crawl_seed.py (bulk async discovery — see
# .github/workflows/host_crawl.yml) — not needed by main.py/discovery.py
aiohttp>=3.9
selectolax>=0.3
duckdb>=0.10
